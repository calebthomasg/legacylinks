import Image from "next/image";import Link from "next/link";import {notFound} from "next/navigation";import {createClient} from "@/utils/supabase/server";import {withReturnPath} from "@/utils/auth/returnPath";import TravelingTokenActivationForm from "@/components/traveling-token/TravelingTokenActivationForm";import TravelingTokenSetupForm from "@/components/traveling-token/TravelingTokenSetupForm";
type State={token_id:string;token_code:string;status:string;is_owner:boolean};type Setup={token_id:string;token_code:string;name:string|null;mission:string|null;status:string;current_box_code:string|null};type Public={token_id:string;token_code:string;name:string|null;mission:string|null;status:string;planter_first_name:string;circulation_started_at:string|null;stop_count:number;total_miles:number;current_box_code:string|null};type Props={params:Promise<{token:string}>};const UUID=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;function Shell({children}:{children:React.ReactNode}){return <main className="relative min-h-screen bg-sand px-6 py-24 sm:py-28"><Link href="/" className="absolute left-6 top-6 sm:left-10 sm:top-8"><Image src="/images/ll-logo.svg" alt="LegacyLink" width={192} height={44} className="h-auto w-40 sm:w-48" priority/></Link><section className="mx-auto w-full max-w-2xl rounded-3xl border border-night-sky/10 bg-white p-8 shadow-sm sm:p-10">{children}</section></main>}
export default async function TravelingTokenPage({params}:Props){const{token}=await params;if(!UUID.test(token))notFound();const supabase=await createClient();const[{data:{user}},{data:state}]=await Promise.all([supabase.auth.getUser(),supabase.rpc("get_traveling_token_activation_state",{p_nfc_public_token:token}).maybeSingle<State>()]);if(!state)notFound();const returnPath=`/t/${token}`;if(state.status==="unclaimed")return <Shell><p className="text-sm font-semibold uppercase tracking-wide text-teal">New Traveling Token</p><h1 className="mt-3 text-4xl font-bold tracking-tight">This traveler is ready to begin.</h1><div className="mt-7 rounded-2xl bg-sand p-6"><p className="text-xs font-bold uppercase tracking-[.14em] text-night-sky/40">Token ID</p><p className="mt-2 font-mono text-2xl font-bold">{state.token_code}</p><p className="mt-3 leading-7 text-night-sky/65">Use the activation PIN included with this token to connect it to your LegacyLink account. Once activated, you'll name it, give it a mission if you'd like, and choose the Treasure Box where its journey begins.</p></div>{user?<TravelingTokenActivationForm token={token} tokenCode={state.token_code}/>:<div className="mt-7"><p className="leading-7 text-night-sky/70">Log in or create an account first. We'll bring you right back to this token.</p><div className="mt-5 flex gap-3"><Link href={withReturnPath("/login",returnPath)} className="button-primary flex-1 justify-center">Log in</Link><Link href={withReturnPath("/signup",returnPath)} className="flex-1 rounded-xl border border-night-sky/20 px-5 py-3 text-center font-semibold">Create account</Link></div></div>}</Shell>;
if(state.is_owner&&state.status==="draft"){const{data:setup,error}=await supabase.rpc("get_owned_traveling_token_setup",{p_nfc_public_token:token}).maybeSingle<Setup>();if(error||!setup)return <Shell><h1 className="text-3xl font-bold">This token belongs to you.</h1><p className="mt-5 rounded-2xl bg-coral/10 p-4 text-sm font-semibold">We couldn't open its setup yet. {error?.message??"Try again."}</p></Shell>;return <Shell><p className="text-sm font-semibold uppercase tracking-wide text-teal">Set up your Traveling Token</p><h1 className="mt-3 text-4xl font-bold tracking-tight">Give this traveler a story to follow.</h1><p className="mt-4 leading-7 text-night-sky/65">Your token stays private until you place it in its first Treasure Box and start its journey.</p><div className="mt-6 rounded-2xl bg-sand p-4"><span className="text-xs font-bold uppercase text-night-sky/40">Token ID</span><p className="mt-1 font-mono text-lg font-bold">{setup.token_code}</p></div><TravelingTokenSetupForm token={token} initial={setup}/></Shell>}
const{data:publicToken}=await supabase.rpc("get_traveling_token_public",{p_nfc_public_token:token}).maybeSingle<Public>();if(!publicToken)notFound();const days=publicToken.circulation_started_at?Math.max(1,Math.floor((Date.now()-new Date(publicToken.circulation_started_at).getTime())/86400000)):0;return <Shell><p className="text-sm font-semibold uppercase tracking-wide text-teal">Traveling Token found</p><h1 className="mt-3 text-4xl font-bold tracking-tight">You found a Traveling Token!</h1><p className="mt-4 leading-7 text-night-sky/65">This token is meant to keep moving from Treasure Box to Treasure Box. Take a look at its journey, then help it continue on its way.</p><div className="mt-7 rounded-2xl bg-sand p-6"><p className="font-mono text-sm font-bold text-night-sky/45">{publicToken.token_code}</p><h2 className="mt-2 text-2xl font-bold">{publicToken.name||"Unnamed Traveler"}</h2>{publicToken.mission&&<p className="mt-3 leading-7 text-night-sky/70">“{publicToken.mission}”</p>}</div><div className="mt-5 grid grid-cols-2 gap-3 sm:grid-cols-4"><Stat label="Planter" value={publicToken.planter_first_name}/><Stat label="Traveling" value={`${days} day${days===1?"":"s"}`}/><Stat label="Stops" value={String(publicToken.stop_count)}/><Stat label="Distance" value={`${Number(publicToken.total_miles||0).toFixed(1)} mi`}/></div><div className="mt-7 rounded-2xl border border-night-sky/10 p-5"><p className="font-bold">Finder logging comes next.</p><p className="mt-2 text-sm leading-6 text-night-sky/60">The next build connects “Log This Find” to the Box ID inside the Treasure Box, then moves the token into your My Treasure as In Transit.</p></div></Shell>}
function Stat({label,value}:{label:string;value:string}){return <div className="rounded-2xl border border-night-sky/10 p-4"><p className="text-[10px] font-bold uppercase tracking-[.1em] text-night-sky/40">{label}</p><p className="mt-1 text-sm font-bold">{value}</p></div>}

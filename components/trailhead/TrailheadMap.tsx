"use client";

import Script from "next/script";
import { useCallback, useEffect, useRef, useState } from "react";

declare global { interface Window { mapboxgl?: { Map:new(options:Record<string,unknown>)=>MapboxMap; Marker:new(options?:Record<string,unknown>)=>MapboxMarker; NavigationControl:new(options?:Record<string,unknown>)=>unknown; GeolocateControl:new(options?:Record<string,unknown>)=>MapboxGeolocateControl; workerUrl?:string; }; } }
type MapboxErrorEvent={error?:{message?:string}};type GeolocateEvent={coords:{latitude:number;longitude:number;accuracy:number}};
type MapboxMap={addControl:(control:unknown,position?:string)=>void;addSource:(id:string,source:Record<string,unknown>)=>void;addLayer:(layer:Record<string,unknown>)=>void;getLayer:(id:string)=>unknown;getSource:(id:string)=>unknown;removeLayer:(id:string)=>void;removeSource:(id:string)=>void;flyTo:(options:Record<string,unknown>)=>void;on:(event:"load"|"error",handler:(()=>void)|((event:MapboxErrorEvent)=>void))=>void;resize:()=>void;remove:()=>void;};type MapboxMarker={setLngLat:(coordinates:[number,number])=>MapboxMarker;addTo:(map:MapboxMap)=>MapboxMarker;remove:()=>void;};type MapboxGeolocateControl={trigger:()=>boolean;on:(event:"geolocate",handler:(event:GeolocateEvent)=>void)=>MapboxGeolocateControl;};
type TrailComment={first_name:string;comment:string;rating:number|null;found_at:string};
type TrailheadCache={cache_id:string;public_code:string;title:string;description:string|null;difficulty:number;terrain:number;chapter_number:number|null;adventure_id:string;adventure_title:string;adventure_slug:string;search_latitude:number;search_longitude:number;search_radius_meters:number;arrival_latitude:number|null;arrival_longitude:number|null;unique_find_count:number;average_rating:number|null;recent_comments:TrailComment[];};type TrailheadMapProps={center:[number,number];caches:TrailheadCache[]};
const MAPBOX_GL_VERSION="3.26.0",MAPBOX_CSP_SCRIPT=`https://api.mapbox.com/mapbox-gl-js/v${MAPBOX_GL_VERSION}/mapbox-gl-csp.js`,MAPBOX_CSP_WORKER="/mapbox-gl-csp-worker.js",EARTH_RADIUS_METERS=6371008.8,SEARCH_SOURCE="active-search-area",SEARCH_FILL="active-search-area-fill",SEARCH_LINE="active-search-area-line";
function searchAreaFeature(cache:TrailheadCache){const lat1=cache.search_latitude*Math.PI/180,lon1=cache.search_longitude*Math.PI/180,distance=cache.search_radius_meters/EARTH_RADIUS_METERS,ring:[number,number][]=[];for(let step=0;step<=64;step++){const bearing=step/64*Math.PI*2,lat2=Math.asin(Math.sin(lat1)*Math.cos(distance)+Math.cos(lat1)*Math.sin(distance)*Math.cos(bearing)),lon2=lon1+Math.atan2(Math.sin(bearing)*Math.sin(distance)*Math.cos(lat1),Math.cos(distance)-Math.sin(lat1)*Math.sin(lat2));ring.push([lon2*180/Math.PI,lat2*180/Math.PI]);}return{type:"Feature",properties:{cache_id:cache.cache_id},geometry:{type:"Polygon",coordinates:[ring]}};}
function distanceMeters(a:number,b:number,c:number,d:number){const p1=a*Math.PI/180,p2=c*Math.PI/180,dp=(c-a)*Math.PI/180,dl=(d-b)*Math.PI/180,x=Math.sin(dp/2)**2+Math.cos(p1)*Math.cos(p2)*Math.sin(dl/2)**2;return EARTH_RADIUS_METERS*2*Math.atan2(Math.sqrt(x),Math.sqrt(1-x));}function zoomForDistance(d:number,r:number){if(d<=Math.max(r*1.5,60))return 18;if(d<=250)return 17;if(d<=750)return 16;if(d<=2000)return 15;if(d<=5000)return 14;return 13;}
function createCacheMarkerElement(cache:TrailheadCache,onSelect:()=>void){const button=document.createElement("button");button.type="button";button.className="group flex flex-col items-center focus:outline-none";button.setAttribute("aria-label",`Open ${cache.title}`);button.addEventListener("click",onSelect);const pin=document.createElement("span");pin.className="flex h-11 w-11 items-center justify-center rounded-full border-[3px] border-white bg-teal text-lg font-black text-white shadow-lg transition-transform group-hover:scale-110";pin.textContent=cache.chapter_number?String(cache.chapter_number):"★";const label=document.createElement("span");label.className="mt-1 max-w-40 rounded-full bg-night-sky px-3 py-1 text-center text-xs font-bold text-white shadow-md";label.textContent=cache.title;button.append(pin,label);return button;}
function Rating({label,value}:{label:string;value:number}){return <div className="min-w-0 flex-1"><div className="flex items-center justify-between gap-3"><span className="text-xs font-semibold uppercase tracking-[.14em] text-night-sky/45">{label}</span><span className="text-sm font-bold text-night-sky">{value}/5</span></div><div className="mt-2 flex gap-1.5">{[1,2,3,4,5].map(s=><span key={s} className={`h-2 flex-1 rounded-full ${s<=value?"bg-teal":"bg-night-sky/10"}`}/>)}</div></div>}
function CacheDetailPanel({cache,onClose,onBegin}:{cache:TrailheadCache;onClose:()=>void;onBegin:()=>void}){const comments=cache.recent_comments||[];return <aside className="absolute inset-x-0 bottom-0 z-30 flex h-[82%] flex-col overflow-hidden rounded-t-[2rem] border-t border-night-sky/10 bg-white shadow-2xl sm:inset-x-4 sm:bottom-4 sm:h-[78%] sm:rounded-[2rem] sm:border lg:inset-y-0 lg:left-0 lg:right-auto lg:h-auto lg:w-[390px] lg:rounded-none lg:border-y-0 lg:border-l-0"><div className="mx-auto mt-2 h-1.5 w-12 shrink-0 rounded-full bg-night-sky/15 lg:hidden"/><div className="flex shrink-0 items-center justify-between border-b border-night-sky/10 px-5 py-4 lg:px-6"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-teal">Adventure</p><p className="mt-1 text-sm font-semibold text-night-sky/65">{cache.adventure_title}</p></div><button onClick={onClose} className="flex h-10 w-10 items-center justify-center rounded-full border border-night-sky/10 text-2xl text-night-sky" aria-label="Close treasure box details">×</button></div><div className="min-h-0 flex-1 overflow-y-auto px-5 py-6 pb-8 lg:px-6">{cache.chapter_number&&<p className="text-xs font-bold uppercase tracking-[.18em] text-coral">Chapter {cache.chapter_number}</p>}<h2 className="mt-2 text-3xl font-bold text-night-sky">{cache.title}</h2>{cache.description&&<p className="mt-3 text-[15px] leading-7 text-night-sky/65">{cache.description}</p>}
<div className="mt-5 flex items-center gap-3 rounded-2xl bg-teal/10 px-4 py-3"><span className="text-xl">✓</span><div><p className="font-bold text-night-sky">{cache.unique_find_count} {cache.unique_find_count===1?"unique find":"unique finds"}</p>{cache.average_rating&&<p className="text-sm text-night-sky/60">★ {cache.average_rating}/5 average adventure rating</p>}</div></div>
<div className="mt-6 flex gap-6 border-y border-night-sky/10 py-5"><Rating label="Difficulty" value={cache.difficulty}/><Rating label="Terrain" value={cache.terrain}/></div><div className="mt-6 rounded-2xl bg-sand p-4"><p className="text-xs font-bold uppercase tracking-[.16em] text-night-sky/45">What to expect</p><p className="mt-2 text-sm leading-6 text-night-sky/70">Begin when you’re ready to hunt for this treasure box. Trailhead will reveal the public search area, while the exact hiding place remains part of the adventure.</p></div><button onClick={onBegin} className="mt-6 w-full rounded-2xl bg-night-sky px-5 py-4 text-sm font-bold text-white">Begin adventure</button>
<section className="mt-7 border-t border-night-sky/10 pt-6"><div className="flex items-end justify-between"><div><p className="text-xs font-bold uppercase tracking-[.16em] text-teal">Trail log</p><h3 className="mt-1 text-xl font-bold text-night-sky">What other trailblazers found</h3></div></div>{comments.length?<div className="mt-4 space-y-3">{comments.map((entry,i)=><article key={`${entry.found_at}-${i}`} className="rounded-2xl bg-sand p-4"><div className="flex items-center justify-between gap-3"><p className="text-sm font-bold text-night-sky">{entry.first_name}</p>{entry.rating&&<span className="text-sm font-bold text-[#b48a13]">★ {entry.rating}/5</span>}</div><p className="mt-2 text-sm leading-6 text-night-sky/70">{entry.comment}</p><p className="mt-2 text-xs text-night-sky/40">{new Date(entry.found_at).toLocaleDateString()}</p></article>)}</div>:<p className="mt-3 text-sm leading-6 text-night-sky/55">No trail notes yet. The first one could be yours.</p>}</section></div></aside>}
function ActiveAdventureCard({cache,onExit}:{cache:TrailheadCache;onExit:()=>void}){return <div className="absolute bottom-4 left-4 right-4 z-30 rounded-[1.5rem] border border-night-sky/10 bg-white/95 p-4 shadow-2xl backdrop-blur sm:left-5 sm:right-auto sm:w-[360px]"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-coral">Adventure in progress</p><h2 className="mt-1 text-xl font-bold text-night-sky">{cache.title}</h2><p className="mt-1 text-sm text-night-sky/60">Find the treasure box within the highlighted area.</p></div><button onClick={onExit} className="rounded-full border border-night-sky/10 px-3 py-2 text-xs font-bold text-night-sky">Exit</button></div></div>}
export default function TrailheadMap({center,caches}:TrailheadMapProps){const containerRef=useRef<HTMLDivElement>(null),mapRef=useRef<MapboxMap|null>(null),geolocateRef=useRef<MapboxGeolocateControl|null>(null),markerRefs=useRef<MapboxMarker[]>([]),activeCacheRef=useRef<TrailheadCache|null>(null),lastAutoZoomRef=useRef(0);const[scriptReady,setScriptReady]=useState(false),[mapLoaded,setMapLoaded]=useState(false),[mapError,setMapError]=useState<string|null>(null),[selectedCache,setSelectedCache]=useState<TrailheadCache|null>(null),[activeCache,setActiveCache]=useState<TrailheadCache|null>(null);const token=process.env.NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN;const clearSearchArea=useCallback(()=>{const map=mapRef.current;if(!map)return;if(map.getLayer(SEARCH_LINE))map.removeLayer(SEARCH_LINE);if(map.getLayer(SEARCH_FILL))map.removeLayer(SEARCH_FILL);if(map.getSource(SEARCH_SOURCE))map.removeSource(SEARCH_SOURCE);},[]);const beginAdventure=useCallback((cache:TrailheadCache)=>{const map=mapRef.current;if(!map)return;clearSearchArea();map.addSource(SEARCH_SOURCE,{type:"geojson",data:{type:"FeatureCollection",features:[searchAreaFeature(cache)]}});map.addLayer({id:SEARCH_FILL,type:"fill",source:SEARCH_SOURCE,paint:{"fill-color":"#14b8a6","fill-opacity":.18}});map.addLayer({id:SEARCH_LINE,type:"line",source:SEARCH_SOURCE,paint:{"line-color":"#0f1d3a","line-width":2.5,"line-opacity":.7,"line-dasharray":[2,2]}});activeCacheRef.current=cache;lastAutoZoomRef.current=0;setSelectedCache(null);setActiveCache(cache);const triggered=geolocateRef.current?.trigger();if(!triggered)map.flyTo({center:[cache.search_longitude,cache.search_latitude],zoom:17,duration:1200,essential:true});},[clearSearchArea]);const exitAdventure=useCallback(()=>{clearSearchArea();activeCacheRef.current=null;setActiveCache(null);},[clearSearchArea]);const initializeMap=useCallback(()=>{if(!scriptReady||!containerRef.current||mapRef.current||!window.mapboxgl||!token)return;try{window.mapboxgl.workerUrl=MAPBOX_CSP_WORKER;const map=new window.mapboxgl.Map({accessToken:token,container:containerRef.current,style:"mapbox://styles/mapbox/streets-v12",center,zoom:12});map.on("load",()=>{map.resize();markerRefs.current=caches.map(cache=>new window.mapboxgl!.Marker({element:createCacheMarkerElement(cache,()=>setSelectedCache(cache)),anchor:"bottom"}).setLngLat([cache.search_longitude,cache.search_latitude]).addTo(map));setMapLoaded(true);setMapError(null);});map.on("error",(e?:MapboxErrorEvent)=>setMapError(e?.error?.message?`Mapbox error: ${e.error.message}`:"Trailhead could not load the map right now."));map.addControl(new window.mapboxgl.NavigationControl({showCompass:true}),"top-right");const geo=new window.mapboxgl.GeolocateControl({positionOptions:{enableHighAccuracy:true},trackUserLocation:true,showUserLocation:true,showAccuracyCircle:true,showUserHeading:true,fitBoundsOptions:{maxZoom:15}});geo.on("geolocate",e=>{const treasure=activeCacheRef.current;if(!treasure)return;const distance=distanceMeters(e.coords.latitude,e.coords.longitude,treasure.search_latitude,treasure.search_longitude),now=Date.now();if(now-lastAutoZoomRef.current<5000)return;lastAutoZoomRef.current=now;map.flyTo({center:[e.coords.longitude,e.coords.latitude],zoom:zoomForDistance(distance,treasure.search_radius_meters),duration:900,essential:true});});map.addControl(geo,"top-right");geolocateRef.current=geo;mapRef.current=map;}catch(e){setMapError(e instanceof Error?`Mapbox error: ${e.message}`:"Trailhead could not load the map right now.");}},[caches,center,scriptReady,token]);useEffect(()=>{if(window.mapboxgl)setScriptReady(true);},[]);useEffect(()=>{initializeMap();return()=>{markerRefs.current.forEach(m=>m.remove());markerRefs.current=[];geolocateRef.current=null;mapRef.current?.remove();mapRef.current=null;};},[initializeMap]);if(!token)return <div className="flex h-full min-h-[520px] items-center justify-center bg-sand"><p className="font-bold text-night-sky">Trailhead needs its Mapbox token.</p></div>;return <div className="relative h-full min-h-[520px] w-full bg-sand"><link href={`https://api.mapbox.com/mapbox-gl-js/v${MAPBOX_GL_VERSION}/mapbox-gl.css`} rel="stylesheet"/><Script src={MAPBOX_CSP_SCRIPT} strategy="afterInteractive" onLoad={()=>setScriptReady(true)} onReady={()=>setScriptReady(true)} onError={()=>setMapError("Trailhead could not load the Mapbox CSP library right now.")}/><div ref={containerRef} className="absolute inset-0 h-full w-full"/>{(!scriptReady||!mapLoaded)&&!mapError&&<div className="pointer-events-none absolute inset-0 flex items-center justify-center bg-sand text-sm font-semibold text-night-sky/60">Opening Trailhead…</div>}{mapError&&<div className="absolute inset-0 flex items-center justify-center bg-sand px-6 text-center"><p className="font-semibold text-night-sky">{mapError}</p></div>}{selectedCache&&!activeCache&&<CacheDetailPanel cache={selectedCache} onClose={()=>setSelectedCache(null)} onBegin={()=>beginAdventure(selectedCache)}/>} {activeCache&&<ActiveAdventureCard cache={activeCache} onExit={exitAdventure}/>}</div>}

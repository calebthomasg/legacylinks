"use client";

import {FormEvent,useEffect,useState} from "react";
import {createClient} from "@/utils/supabase/client";

type Props={experienceId:string;onCancel:()=>void;onSaved:()=>void};
type PersonResult={person_id:string;linked_user_id:string|null;display_name:string;first_name:string;last_name:string|null;birth_date:string|null;death_date:string|null;profile_summary:string|null;bio:string|null;profile_photo_path:string|null};
type EditorRow={experience_id:string;title:string;description:string|null;status:string;visibility:'public'|'hidden';memorial_type:string;subject_name:string|null;biography:string|null;latitude:number;longitude:number;linked_people:PersonResult[]};
const TYPES=[['headstone','Headstone'],['memorial','Memorial'],['monument','Monument'],['historic_site','Historic Site'],['event','Event']] as const;

export default function MemorialEditForm({experienceId,onCancel,onSaved}:Props){
 const supabase=createClient();
 const[loading,setLoading]=useState(true);const[saving,setSaving]=useState(false);const[error,setError]=useState<string|null>(null);
 const[title,setTitle]=useState('');const[type,setType]=useState('headstone');const[description,setDescription]=useState('');const[biography,setBiography]=useState('');const[latitude,setLatitude]=useState('');const[longitude,setLongitude]=useState('');const[visibility,setVisibility]=useState<'public'|'hidden'>('public');const[published,setPublished]=useState(true);
 const[personQuery,setPersonQuery]=useState('');const[birthDate,setBirthDate]=useState('');const[searching,setSearching]=useState(false);const[results,setResults]=useState<PersonResult[]>([]);const[selected,setSelected]=useState<PersonResult[]>([]);
 useEffect(()=>{let active=true;(async()=>{const{data,error:loadError}=await supabase.rpc('get_memorial_editor',{p_experience_id:experienceId});if(!active)return;if(loadError){setError(loadError.message);setLoading(false);return}const row=((data??[])[0]??null) as EditorRow|null;if(!row){setError('This memorial could not be loaded.');setLoading(false);return}setTitle(row.title||'');setType(row.memorial_type);setDescription(row.description||'');setBiography(row.biography||'');setLatitude(String(row.latitude));setLongitude(String(row.longitude));setVisibility(row.visibility);setPublished(row.status==='published');setSelected(row.linked_people||[]);setLoading(false)})();return()=>{active=false}},[experienceId]);
 async function searchPeople(){setError(null);setSearching(true);const{data,error:searchError}=await supabase.rpc('search_my_family_people',{p_query:personQuery.trim()||null,p_birth_date:birthDate||null,p_limit:20});setSearching(false);if(searchError){setError(searchError.message);return}setResults((data??[]) as PersonResult[])}
 function togglePerson(person:PersonResult){const alreadySelected=selected.some(p=>p.person_id===person.person_id);if(alreadySelected){setSelected(current=>current.filter(p=>p.person_id!==person.person_id));return}setSelected(current=>[...current,person]);setPersonQuery('');setBirthDate('');setResults([])}
 function dynamicName(){return selected.map(p=>p.display_name).join(' & ')}
 async function submit(e:FormEvent){e.preventDefault();setError(null);const lat=Number(latitude),lng=Number(longitude);if(!title.trim()&&!selected.length){setError('Add at least one family profile or give this memorial a title.');return}if(!Number.isFinite(lat)||lat<-90||lat>90||!Number.isFinite(lng)||lng<-180||lng>180){setError('Enter valid latitude and longitude coordinates.');return}setSaving(true);const{error:saveError}=await supabase.rpc('update_memorial_experience',{p_experience_id:experienceId,p_title:title.trim()||null,p_description:description.trim()||null,p_memorial_type:type,p_biography:biography.trim()||null,p_latitude:lat,p_longitude:lng,p_visibility:visibility,p_publish:published,p_person_ids:selected.map(p=>p.person_id)});setSaving(false);if(saveError){setError(saveError.message);return}onSaved()}
 const field='mt-2 w-full rounded-2xl border border-night-sky/15 bg-white px-4 py-3 text-sm text-night-sky outline-none focus:border-teal';
 if(loading)return <div className="flex h-full items-center justify-center bg-white text-sm font-semibold text-night-sky/55">Loading memorial…</div>;
 return <div className="flex h-full min-h-0 flex-col bg-white"><div className="flex items-center gap-3 border-b border-night-sky/10 px-5 py-4"><button onClick={onCancel} className="flex h-9 w-9 items-center justify-center rounded-full border border-night-sky/10 text-xl">←</button><div><p className="text-xs font-bold uppercase tracking-[.18em] text-teal">My Treasure</p><h2 className="font-bold text-night-sky">Edit Memorial</h2></div></div><form onSubmit={submit} className="min-h-0 flex-1 overflow-y-auto px-5 py-5"><section className="rounded-2xl border border-night-sky/10 p-4"><p className="text-xs font-bold uppercase tracking-[.12em] text-teal">Family tree profiles</p><h3 className="mt-1 font-bold text-night-sky">Who is remembered here?</h3><p className="mt-2 text-xs leading-5 text-night-sky/50">Add or remove people at any time. Linked names and family-tree bios stay dynamic on Trailhead.</p>{selected.length>0&&<div className="mt-3 space-y-2">{selected.map(person=><div key={person.person_id} className="flex items-center justify-between rounded-xl bg-teal/10 px-3 py-2"><div><p className="text-sm font-bold text-night-sky">{person.display_name}</p><p className="text-xs text-night-sky/45">{person.birth_date||'Birth date unknown'}{person.death_date?` – ${person.death_date}`:''}</p></div><button type="button" onClick={()=>togglePerson(person)} className="text-xs font-bold text-coral">Remove</button></div>)}<p className="rounded-xl border border-dashed border-teal/40 px-3 py-2 text-xs font-semibold text-night-sky/55">Search below to add another person.</p></div>}<div className="mt-4 grid gap-2"><input value={personQuery} onChange={e=>setPersonQuery(e.target.value)} placeholder={selected.length?'Search for another person':'Search by name, Person ID, or linked User ID'} className={field}/><div className="grid grid-cols-[1fr_auto] gap-2"><input type="date" value={birthDate} onChange={e=>setBirthDate(e.target.value)} className={field}/><button type="button" onClick={searchPeople} disabled={searching} className="mt-2 rounded-2xl bg-night-sky px-4 text-sm font-bold text-white disabled:opacity-50">{searching?'Searching…':'Search'}</button></div></div>{results.length>0&&<div className="mt-3 max-h-48 overflow-y-auto rounded-xl border border-night-sky/10">{results.map(person=>{const chosen=selected.some(p=>p.person_id===person.person_id);return <button key={person.person_id} type="button" onClick={()=>togglePerson(person)} className={`flex w-full items-center justify-between border-b border-night-sky/5 px-3 py-3 text-left last:border-0 ${chosen?'bg-teal/10':'bg-white'}`}><div><p className="text-sm font-bold text-night-sky">{person.display_name}</p><p className="text-xs text-night-sky/45">{person.birth_date||'Birth date unknown'}{person.death_date?` – ${person.death_date}`:''}</p></div><span className="text-xs font-bold text-teal">{chosen?'Added':'Add'}</span></button>})}</div>}</section><label className="mt-5 block text-xs font-bold uppercase tracking-[.12em] text-night-sky/55">Memorial type<select value={type} onChange={e=>setType(e.target.value)} className={field}>{TYPES.map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label><label className="mt-4 block text-xs font-bold uppercase tracking-[.12em] text-night-sky/55">Manual title <span className="normal-case tracking-normal text-night-sky/35">fallback when no profiles are linked</span><input value={title} onChange={e=>setTitle(e.target.value)} className={field}/></label>{selected.length>0&&<div className="mt-4 rounded-2xl bg-sand p-4"><p className="text-xs font-bold uppercase tracking-[.12em] text-night-sky/45">Trailhead title preview</p><p className="mt-1 font-bold text-night-sky">{dynamicName()}</p></div>}<label className="mt-4 block text-xs font-bold uppercase tracking-[.12em] text-night-sky/55">Manual short description<textarea value={description} onChange={e=>setDescription(e.target.value)} rows={3} className={field}/></label><label className="mt-4 block text-xs font-bold uppercase tracking-[.12em] text-night-sky/55">Memorial-specific bio or story<textarea value={biography} onChange={e=>setBiography(e.target.value)} rows={5} className={field}/></label><div className="mt-5 rounded-2xl bg-sand p-4"><p className="text-xs font-bold uppercase tracking-[.12em] text-night-sky/55">Location</p><div className="mt-3 grid grid-cols-2 gap-3"><label className="text-xs font-semibold text-night-sky/55">Latitude<input inputMode="decimal" value={latitude} onChange={e=>setLatitude(e.target.value)} className={field}/></label><label className="text-xs font-semibold text-night-sky/55">Longitude<input inputMode="decimal" value={longitude} onChange={e=>setLongitude(e.target.value)} className={field}/></label></div></div><div className="mt-5"><p className="text-xs font-bold uppercase tracking-[.12em] text-night-sky/55">Trailhead visibility</p><div className="mt-2 grid grid-cols-2 gap-2"><button type="button" onClick={()=>setVisibility('public')} className={`rounded-2xl border p-3 text-sm font-bold ${visibility==='public'?'border-teal bg-teal/10':'border-night-sky/10 text-night-sky/50'}`}>Public</button><button type="button" onClick={()=>setVisibility('hidden')} className={`rounded-2xl border p-3 text-sm font-bold ${visibility==='hidden'?'border-teal bg-teal/10':'border-night-sky/10 text-night-sky/50'}`}>Hidden</button></div></div><label className="mt-5 flex items-center gap-3 rounded-2xl border border-night-sky/10 p-4"><input type="checkbox" checked={published} onChange={e=>setPublished(e.target.checked)} className="h-4 w-4"/><span><span className="block text-sm font-bold text-night-sky">Published</span><span className="block text-xs text-night-sky/45">Turn this off to keep the memorial as a draft.</span></span></label>{error&&<div className="mt-5 rounded-2xl bg-coral/10 p-3 text-sm font-semibold text-night-sky">{error}</div>}<button disabled={saving} className="mt-6 w-full rounded-2xl bg-night-sky px-5 py-4 text-sm font-bold text-white disabled:opacity-50">{saving?'Saving…':'Save Changes'}</button></form></div>
}